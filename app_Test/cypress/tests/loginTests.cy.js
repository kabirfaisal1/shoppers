describe('Test to login', () => {
  it('Should be able to submit a successful submission via contact us form', () => {
    cy.visit('');
    //cy.get('#contact-us').click({force: true})
    //email
    cy.get('[id="<TO DO>"]').type('');
    //passed
    cy.get('[id="<TO DO>"]').type('');
    //click on log in
    cy.get('[id="<TO DO>"]').click();
  });

  it('', () => {
    //cypress code
  });
});
